#include<iostream>
#include <stdlib.h>
#include <stdio.h>

using namespace std;

class fdisk
{
public:
    fdisk(int size, char unit, string path,char type, string fit, string dilit, string name,int add, string mov);
    ~fdisk();
};

fdisk::fdisk(int size, char unit, string path,char type, string fit, string dilit, string name,int add, string mov){
                
}

fdisk::~fdisk(){}